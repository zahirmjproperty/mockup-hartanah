# Zentra Asset — design preview (unlisted)

Static preview of the Zentra Asset landing page concept: **modern, futuristic, luxury**
(deep navy ground + metallic gold accent).

Not a real system. No backend, no data, no login. Published only so it can be reviewed
and clicked through before any production build.

- `index.html` — the landing page
- `assets/zentra.css` — the Zentra Design System stylesheet
- `DESIGN.md` — the design-token spec (lint-clean); edit this, then re-export tokens
